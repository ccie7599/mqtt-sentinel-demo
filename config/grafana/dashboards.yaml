apiVersion: 1

providers:
  - name: 'MQTT Sentinel'
    orgId: 1
    folder: 'MQTT Sentinel'
    folderUid: 'mqtt-sentinel'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/sentinel
